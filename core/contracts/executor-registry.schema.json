{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "core/contracts/executor-registry.schema.json",
  "title": "ExecutorRegistry Contract",
  "description": "Canonical registry of declared executor manifests. Registry entries are schema-valid manifests only; aliases may not shadow canonical executor IDs (no alias field exists), and registration never implies qualification or authority.",
  "type": "object",
  "additionalProperties": false,
  "required": ["registry_id", "executors"],
  "properties": {
    "registry_id": { "type": "string", "pattern": "^execreg_[a-z0-9_]+$" },
    "version": { "type": "string", "minLength": 1 },
    "executors": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "executor-manifest.schema.json" }
    }
  }
}
