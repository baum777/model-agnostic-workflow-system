# Commands Run

```text
pi \
  --provider minimax \
  --model MiniMax-M3 \
  --no-session \
  --print \
  "Create a concise Markdown proposal for the Baum-OS Pi Tier-1 draft-only workflow. ..."
```

No `--api-key` passed.
No secrets in command.
No tools invoked by Pi.
Exit code: 0
