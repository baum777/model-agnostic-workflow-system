# Snake Example

Classic Snake implemented as a small static example using the repo's existing Node tooling.

## Run

```bash
npm run snake:dev
```

Open the reported URL, then play with:

- arrow keys or WASD
- `R` or the Restart button
- touch controls on coarse-pointer devices

## Test

```bash
npm run snake:test
```

