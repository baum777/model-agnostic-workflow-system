# Astra Safety Harness

Status: PREPARED_NOT_ADOPTED. Dedicated owner-authorized candidate subtree.
Generic mechanics consume target-owned policy; this subtree owns no UNITERA architecture.

Run with Python 3 and requirements.txt dependencies. Tests:
`PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s astra-safety-harness/tests -v`

Binding: config/binding.json. Real Unitera execution is not authorized.
No commit, push, branch, release, remediation or waiver authority is granted.

Implementation receipts live in evidence/. Run outputs are separate from implementation receipts.
