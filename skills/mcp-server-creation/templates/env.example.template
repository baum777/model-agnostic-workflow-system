# Required
PORT=3000
LOG_LEVEL=info
MCP_SERVER_NAME=replace-me-mcp-server
MCP_SERVER_VERSION=0.1.0
MCP_AUTH_MODE=noauth
UPSTREAM_BASE_URL=https://api.example.com

# Optional or auth-specific
# UPSTREAM_API_KEY=
# OAUTH_ISSUER_URL=
# OAUTH_AUDIENCE=
# OAUTH_CLIENT_ID=
# OAUTH_CLIENT_SECRET=

# Notes
# - Do not commit real credentials.
# - Document which variables are required for each deployment environment.
# - If mixed auth is used, document which tools require which identity path.
